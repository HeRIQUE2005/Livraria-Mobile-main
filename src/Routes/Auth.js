import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Auth = () => {
  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  )
}

export default Auth

const styles = StyleSheet.create({})